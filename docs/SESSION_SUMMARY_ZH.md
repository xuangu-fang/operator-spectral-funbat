# 本轮会话总结（2026-08-19）

<!-- STATUS BANNER -- keep at the top -->
> ## ⚠ 历史档案，不是当前口径
>
> **当前有效的技术口径见 [`HANDOVER_TECHNICAL_ZH.md`](HANDOVER_TECHNICAL_ZH.md)。**
>
> 本文写于主线转向**受限传感器布局**之前，其中的结论有一部分已被后续实验推翻：
>
> | 本文可能出现的说法 | 现状 |
> |---|---|
> | 随机掩码下相对通用字典 **+5.5% / +8.0%** | **打平**。该 margin 建立在只扫了 3 个长度尺度的 Matérn 上；网格放宽后 baseline 自己的最优在 $\ell = 2.4$ |
> | 单面墙 **+17.2%** | **已撤回**，同一原因。修正后 0.5448 vs 0.5449 |
> | "只需要方程的形式" | 不准确。主表用的是**单个点估计（错 1.5 倍）走 K2 的机器**，真正的 K1（范围 + 池化）见交接文稿 §2.7 |
> | 主指标是随机掩码下的重构 | 主指标已改为**受限布局**下的重构，核心量是**调参代价** |
>
> 保留本文是为了记录推导与失败路径，**不要从这里取数字**。


> 供 10 小时后回来时快速接续。全部改动已推送 `main`。

---

## 1. 一句话现状

**主张已经站住，且统计很硬。** 方法比会话开始时**更简单**，数据换成了独立数值求解器，formulation 修订三次。

---

## 2. 核心结果

15 seeds，独立 forced-PDE 求解器场，所有 arm 共享场/mask/噪声/宿主/rank/优化器/step 预算，**且各自使用对自己最优的 routing（两边都是 global）**：

| 观测率 | **PDE 形式 kernel（本文）** | 通用字典 | 最近邻 | paired wins |
|---|---:|---:|---:|---:|
| **1%** | **0.4217 ± 0.0470** | 0.4464 ± 0.0545 | 0.5699 | **13/15** |
| 2% | **0.3478** | 0.3581 | 0.4929 | **14/15** |
| 5% | **0.2969** | 0.2993 | 0.4073 | **13/15** |

1% 处配对 margin `+0.0247 ± 0.0235`（相对 5.5%），符号检验 $p=0.0037$，Wilcoxon $p=0.00058$。

其他 baseline（1%）：离散 CP / Tucker 补全 $\ge 0.98$（**不优于预测均值**）；kernel ridge（oracle 调长度尺度）0.4887；满观测 Tucker 天花板 0.1791。

> ⚠ **本轮后期纠正的一个错误**：更早的主表让两边都用 `per_mode_rank` routing，报出 15/15、相对 8.0%、$p=3\times10^{-5}$。消融显示该设置在 1% 下对两个 bank 都过拟合，且**对 generic 的伤害是对 operator 的两倍**（0.042 vs 0.023），因此**把效应高估了约 1.6 倍**。旧数字保留在 `headline_legacy_pmr_summary.json`。
>
> 教训：**"参数匹配"不等于"配置匹配"**。rank、预算、系数量都对齐了，但共用一个对 baseline 更不利的超参设置，依然是不公平比较。

## 3. 方法（最终形态，比开始时简单得多）

1. 由算子符号得联合谱 $S_{\mathrm{op}}=\lvert\widehat{\mathcal L}\rvert^{-2}S_w$，**只用形式，系数取 nominal**；
2. **非负低秩分离**成 $Q=4$ 个逐维原子 $s_{dq}$；
3. 学一个**全局混合权重**（4 个 logit，所有 mode 与 rank 共享），得每个 mode 的谱；该谱**就是**核：$k(x,x')=\sum_k s(k)\psi_k(x)\psi_k(x')$，对任意非负 $s$ 均 PSD。

**可学 kernel 参数只有 4 个。** per-mode/rank routing 在 1% 观测下过拟合（+0.023），不采用。

> ⚠ 会话中期曾错误地认为"单个边缘谱即可，分离和 routing 可全部删除"。那是在 per-mode/rank 下比较得出的，该设置拖累了两个 arm。同一协议下 global routing 的正确结果：分离 `0.4383` vs 单个边缘谱 `0.4631`，**分离确实有用**。

---

## 4. 三次 formulation 修订（都由数据推翻既有设定）

| 修订 | 触发 | 结果 |
|---|---|---|
| CP → **Tucker** 宿主，逐 mode 不同 rank | 真实二维场满观测 CP rank-10 只能到 0.32，而多线性秩仅 [2,11,11] | 二维场是团块不是外积；[2,11,11] 的 core 242 参数 vs 等秩 11 的 1331 |
| 周期 Fourier → **边界匹配本征基** | 初值数据时间维首末帧跳变是相邻步长的 5.6 倍 | 算子本征基由边界条件决定；无流边界下是余弦。同一次运行 NRMSE 1.30 → 1.08 |
| 模型自采样 → **独立数值求解器** | 此前全部正面结果的真值都采样自模型自身先验 | 场携带完整不可分联合谱，方法只见其边缘化近似 + 故意取错的系数 |

---

## 5. 四条被结构性理由排除的路（全部可事先判定）

| 方向 | 证据 |
|---|---|
| Kolmogorov 湍流 | 满观测 CP rank-40 仍 0.298；谱宽，算子不压制高频 |
| PDEBench 2D diff-react | 沿每个空间轴需 63% 的模才够 95% 能量，对分辨率与降采样方式均不变 |
| **带通 Swift–Hohenberg 算子** | **与直觉相反是最坏情形**：响应在环上取极小，环最不可分。1% 时 0.92 vs 通用 0.74 |
| CFDBench cavity 单 case | rank95 = [1,2,2]，rank-1 平凡任务 |

---

## 6. 一个求解器 bug（值得记住的方法论）

块状强迫（`np.kron`）的谱是 sinc 型、带零点，而先验假设光滑高斯 $S_w$。于是**先验与数据的分歧落在强迫上而非算子上**，任何算子都不可能好、任何光滑先验都一样平庸。改为高斯滤波白噪声后，最不相似两 mode 的谱 cosine 从 0.39 升到 0.85。

**教训：比较先验之前，先验证数据的谱确实符合先验族假设的生成机制。**

---

## 7. 交付物

| 类型 | 位置 |
|---|---|
| 结果与数值 | `docs/RESULTS_LOG_ZH.md`、`results/forced_pde/` |
| formulation 修订史 | `docs/PAPER_TECHNICAL_REPORT_ZH.md` 第 14 节 |
| 论文 LaTeX（七节齐全） | `paper/`，`cd paper && make` |
| 图 | `results/forced_pde/figure_{headline,main,baselines}.png` |
| 求解器与实验 | `experiments/forced_pde_solver.py`、`run_forced_pde_*.py` |
| 测试 | 19 个，全过；冻结的旧结果仍逐位复现（最大差 1.1e-07） |

---

## 8. 关键的方法论教训（三条，都由实际踩坑得出）

1. **比较设计**：做方法层取舍或基线比较时，**每个候选必须在它自己的最优设置下评估**。共用一个对双方都不利的设置，会让差异被共同的劣化掩盖。本轮因此犯了两次错（主表高估 1.6 倍；错误地宣布分离可删除），已写进 `run_no_routing_comparison.py` 的代码注释。「参数匹配」是必要条件，不是充分条件。
2. **先验 vs 数据**：比较先验之前，**先验证数据的谱确实符合先验族假设的生成机制**。块状强迫的 sinc 谱零点让分歧落在强迫而非算子上，结果任何算子都不好、任何光滑先验都一样平庸。
3. **可行性先于比较**：**满观测下的低秩天花板必须先测**。若它不明显低于 1，任何 kernel 比较都测不出东西——这是拒掉 Kolmogorov 与 PDEBench 的判据。

## 9. 完整证据清单（全部已跑完并落盘）

| 证据 | 结果 | 文件 |
|---|---|---|
| **主表**（15 seeds，公平配置） | 1%/2%/5% 分别 13/15、14/15、13/15；1% 处 5.5%，$p=0.0037$ | `fair_global_summary.json` |
| **Baseline** | 离散 CP/Tucker 在 1% 时 $\ge 0.98$（不优于均值）；oracle 调参 KRR 0.4887 | `baselines_summary.json` |
| **消融**（14 arms） | 错误算子族更差；系数容忍不对称；加倍 atom 更糟；floor 是纯代价 | `ablation_fixed_summary.json` |
| **负控制：各向同性** | margin 从 +0.020 塌到 **+0.005**（3/5） | `operators_summary.json` |
| **oracle 对照** | 落后 oracle 逐 mode 调参核仅 **3.9%**；共享调参 oracle 反而输给我们 | `no_routing_1200_summary.json` |
| **UQ** | 校准也更好且优势同样集中在稀疏端；但两者在 1–2% 都欠覆盖 | `uq_summary.json` |
| **设计消融** | 分离值 0.025；global 混合优于 per-mode/rank 0.023 | `no_routing_1200_summary.json` |

**两条互相独立、指向同一机制的证据**：各向同性负控制（margin 塌陷）与 oracle 分解（优势全部来自逐 mode 调参）。二者都说明——**先验的价值来自"逐维的谱必须不同"**。

## 10. 尚未完成 / 建议下一步

1. **论文剩余 6 个 todo**：都是引用与附录搬运（related work、附录 A 数值表、A2 公平性表），不影响结论。
2. **公平版的 2%/5% 消融**：目前消融只在 1% 做过。
3. **更多 seeds 用于 oracle 对照与负控制**：目前均为 5 seeds（主表已 15 seeds）。
4. **真实数据**：所有现成候选都未过低秩判据。若要补，需要找**耗散主导且大尺度**的真实场；否则维持"独立求解器 + 完整不可分谱"这一定位，并在 limitation 中明确。
5. **修复 UQ 欠覆盖**：属宿主模型问题（如 full-covariance posterior 或 noise 先验），与本文主张正交。
5. 若要真实数据：目前所有现成候选都未过低秩判据，需要找**耗散主导且大尺度**的真实场，或接受"独立求解器 + 完整不可分谱"这一定位。
