\section{Experiments}
\label{sec:experiments}

\subsection{Setup}
Ground truth comes from an independent finite-difference integration of a
stochastically forced linear PDE run to statistical steady state, so the field
carries the operator's true, fully non-separable joint spectrum while the method
sees only the marginals of a nominal-coefficient version of it.  Nothing is
sampled from the model's own prior.  The tensor is $[t,x,y]$ at $32^3$; all three
modes are PDE coordinates, so every kernel comes from the equation and no mode
needs a fallback.  We observe $1\%$, $2\%$ or $5\%$ of entries uniformly at
random and reconstruct the rest.

\paragraph{Feasibility is checked before comparing anything.}
A fully observed Tucker fit at the ranks used reaches $0.17$--$0.21$ relative
error.  This ceiling is shared by every arm; where it is not comfortably below
$1$, no kernel can matter, and we use it as the criterion that rejected two
datasets (\Cref{sec:limitations}).

\subsection{Main result}

\begin{table}[t]
  \centering
  \caption{Held-out NRMSE, fifteen seeds, identical fields and masks across arms, each arm at the routing best for it.}
  \label{tab:main}
  \small
  \begin{tabular}{lcccc}
    \toprule
    Observed & PDE-form kernels (ours) & generic dictionary & nearest neighbour & paired wins \\
    \midrule
    $1\%$ & \textbf{0.4217} & 0.4464 & 0.5699 & 13/15 \\
    $2\%$ & \textbf{0.3478} & 0.3581 & 0.4929 & 14/15 \\
    $5\%$ & \textbf{0.2969} & 0.2993 & 0.4073 & 13/15 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.92\linewidth]{figure_main.png}
  \caption{The margin over a parameter-matched generic dictionary falls from
  $0.040$ at $1\%$ observed to $0.003$ at $5\%$.  A prior should matter where the
  data does not determine the answer and stop mattering once it does; a large
  claimed advantage in the right-hand bar would be grounds for suspicion.}
  \label{fig:main}
\end{figure}

\paragraph{Pooled over fifteen seeds at $1\%$.}
PDE-form kernels reach $0.4217 \pm 0.0470$ against $0.4464 \pm 0.0545$ for the
parameter-matched generic dictionary and $0.5699$ for nearest neighbour, with a
fully observed Tucker ceiling of $0.1791$.  The paired margin is
$0.0247 \pm 0.0235$, a $5.5\%$ relative improvement on 13 of 15 seeds; the sign
test gives $p = 0.004$ and a Wilcoxon signed-rank test $p = 0.0006$.  The test is
paired because all arms share the field, mask and noise within a seed.

\paragraph{Both arms are run at their own best setting.}
An earlier version of this table used per-mode/rank routing for both arms and
reported $15/15$ with an $8.0\%$ margin.  The ablation shows that routing
over-fits at $1\%$ observed and costs the generic dictionary $0.042$ against
$0.023$ for the operator bank, so that setting handicaps the baseline more than
it handicaps us and overstates the effect by roughly $1.6\times$.  We therefore
give each arm the routing that is best for it, which is global for both.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.92\linewidth]{figure_headline.png}
  \caption{Every one of the fifteen seeds falls below the diagonal.  The effect
  is modest in size and completely consistent in sign, which is the honest
  characterisation of what a correct prior buys at this sparsity.}
  \label{fig:headline}
\end{figure}

\subsection{Baselines}
\todo{table from baselines\_summary.json on corrected forcing}
Discrete low-rank completion (CP and Tucker with EM imputation) does not beat
predicting the mean below $2\%$ observed, which is the evidence that continuous
function factors are necessary rather than decorative.  The strongest competitor
is kernel ridge regression whose length scale is selected on held-out error -- an
advantage no deployable method has.

\subsection{Where the gain comes from}
\todo{ablation table from ablation\_fixed\_summary.json}
The arms vary only the spectral bank or the routing: the exact operator spectrum
without any low-rank projection, the right form with coefficients scaled between
$0.1\times$ and $10\times$, an isotropic version of the right form, wrong operator
families, generic banks at matched and doubled size, and global versus
per-mode/rank routing.

One result is worth stating in the main text because it simplifies the method: a
single fixed marginal spectrum per mode, with no learnable kernel parameters at
all, matches the rank-4 separation with learned routing ($0.4631$ versus
$0.4613$ over five seeds, against a seed-to-seed spread of $0.025$).  The
dictionary and routing machinery can therefore be removed entirely.

\subsection{A derived kernel versus a tuned one}
\todo{no\_routing\_summary.json}
The sharpest form of the question is whether reading the kernel off the equation
gets you where tuning a kernel on the answer would.
