# Excluded pre-confirmation artifacts

These seed 101--103 files were produced while diagnosing the strict-zero sqrt
gradient and routing learning-rate instability.  Some contain non-finite values
or superseded optimizer settings.  They are retained only for provenance and
are excluded from every summary, figure and claim.  Valid development results
are in `../escape_floor_operator_init_development/`; untouched confirmation
uses only seeds 201--205 in `../submission_confirmation/`.
